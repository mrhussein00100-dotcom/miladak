# دليل التحكم في جداول قاعدة البيانات

## 📊 **نعم! جميع الجداول موجودة ويمكن التحكم بها**

### 🗄️ **الجداول المتاحة:**

## 📜 **الأحداث التاريخية**

### الجداول:

| الجدول         | الوصف                | الحقول                                      |
| -------------- | -------------------- | ------------------------------------------- |
| `daily_events` | أحداث يومية متكررة   | day, month, title, description, category    |
| `major_events` | أحداث رئيسية للسنوات | year_id, title, description, date, category |

### APIs للتحكم:

- **GET** `/api/daily-events/[month]/[day]` - جلب أحداث يوم معين
- **POST** `/api/daily-events` - إضافة حدث جديد
- **PUT** `/api/daily-events/[id]` - تحديث حدث
- **DELETE** `/api/daily-events/[id]` - حذف حدث

### مثال إضافة حدث:

```json
POST /api/daily-events
{
  "day": 1,
  "month": 1,
  "title": "رأس السنة الميلادية",
  "description": "بداية السنة الجديدة",
  "category": "ثقافي"
}
```

---

## ⭐ **المشاهير**

### الجداول:

| الجدول            | الوصف                    | الحقول                                   |
| ----------------- | ------------------------ | ---------------------------------------- |
| `daily_birthdays` | مشاهير حسب تاريخ الميلاد | day, month, birth_year, name, profession |

### APIs للتحكم:

- **GET** `/api/daily-birthdays/[month]/[day]` - جلب مشاهير يوم معين
- **POST** `/api/daily-birthdays` - إضافة مشهور جديد
- **PUT** `/api/daily-birthdays/[id]` - تحديث بيانات مشهور
- **DELETE** `/api/daily-birthdays/[id]` - حذف مشهور

### مثال إضافة مشهور:

```json
POST /api/daily-birthdays
{
  "day": 15,
  "month": 6,
  "birth_year": 1949,
  "name": "أحمد زكي",
  "profession": "ممثل مصري"
}
```

---

## 💎 **أحجار وزهور الميلاد**

### الجداول:

| الجدول          | الوصف                   | الحقول                                          |
| --------------- | ----------------------- | ----------------------------------------------- |
| `birthstones`   | أحجار الميلاد حسب الشهر | month, stone_name, stone_name_ar, description   |
| `birth_flowers` | زهور الميلاد حسب الشهر  | month, flower_name, flower_name_ar, description |

### APIs للتحكم:

- **GET** `/api/monthly-info/[month]` - جلب معلومات الشهر (حجر + زهرة + لون)
- **PUT** `/api/monthly-info/[month]` - تحديث معلومات الشهر

### مثال تحديث حجر الميلاد:

```json
PUT /api/monthly-info/1
{
  "birthstone": {
    "stone_name": "Garnet",
    "stone_name_ar": "العقيق",
    "description": "حجر القوة والحماية"
  }
}
```

---

## 🎨 **الألوان والأرقام**

### الجداول:

| الجدول           | الوصف                  | الحقول                                     |
| ---------------- | ---------------------- | ------------------------------------------ |
| `lucky_colors`   | ألوان محظوظة حسب الشهر | month, color_name, color_name_ar, hex_code |
| `chinese_zodiac` | أبراج صينية            | year, name, name_ar, description           |

### APIs للتحكم:

- **GET** `/api/monthly-info/[month]` - جلب اللون المحظوظ للشهر
- **GET** `/api/chinese-zodiac/[year]` - جلب البرج الصيني للسنة
- **PUT** `/api/monthly-info/[month]` - تحديث اللون المحظوظ

### مثال تحديث لون محظوظ:

```json
PUT /api/monthly-info/1
{
  "luckyColor": {
    "color_name": "Blue",
    "color_name_ar": "الأزرق",
    "hex_code": "#3B82F6"
  }
}
```

---

## 🔧 **طرق التحكم في البيانات:**

### 1. **عبر APIs (الطريقة المفضلة)**

```bash
# جلب البيانات
curl http://localhost:3000/api/daily-events/1/1

# إضافة بيانات
curl -X POST http://localhost:3000/api/daily-events \
  -H "Content-Type: application/json" \
  -d '{"day":1,"month":1,"title":"حدث جديد"}'
```

### 2. **عبر دوال قاعدة البيانات**

```typescript
import { queryAll, execute } from '@/lib/db/unified-database';

// جلب الأحداث
const events = queryAll(
  'SELECT * FROM daily_events WHERE month = ? AND day = ?',
  [1, 1]
);

// إضافة حدث
execute('INSERT INTO daily_events (day, month, title) VALUES (?, ?, ?)', [
  1,
  1,
  'حدث جديد',
]);
```

### 3. **عبر واجهة الإدارة**

```bash
# تشغيل واجهة الإدارة
node scripts/database-admin.js
```

### 4. **عبر ملفات البيانات المحلية**

- `lib/dailyData.ts` - الأحداث والمشاهير
- `lib/celebrities.ts` - قاعدة بيانات المشاهير
- `lib/calculations/zodiacCalculations.ts` - الأبراج الصينية

---

## 📈 **إحصائيات الجداول:**

| الصفحة              | الجداول                          | حالة البيانات | إمكانية التحكم |
| ------------------- | -------------------------------- | ------------- | -------------- |
| 📜 أحداث تاريخية    | `daily_events`, `major_events`   | ✅ متاحة      | ✅ كاملة       |
| ⭐ مشاهير           | `daily_birthdays`                | ✅ متاحة      | ✅ كاملة       |
| 💎 أحجار وزهور      | `birthstones`, `birth_flowers`   | ✅ متاحة      | ✅ كاملة       |
| 🎨 الألوان والأرقام | `lucky_colors`, `chinese_zodiac` | ✅ متاحة      | ✅ كاملة       |

---

## 🎯 **الخلاصة:**

### ✅ **نعم، يمكن التحكم الكامل في جميع البيانات!**

1. **جداول موجودة** - جميع الجداول المطلوبة متاحة
2. **APIs جاهزة** - واجهات برمجية للقراءة والكتابة
3. **دوال مساعدة** - دوال TypeScript للتعامل مع البيانات
4. **واجهة إدارة** - أدوات للإدارة والصيانة
5. **نسخ احتياطية** - حماية البيانات

### 🚀 **يمكنك الآن:**

- إضافة أحداث تاريخية جديدة
- إدارة قائمة المشاهير
- تحديث أحجار وزهور الميلاد
- تخصيص الألوان والأرقام المحظوظة
- إنشاء نسخ احتياطية
- تصدير واستيراد البيانات

**قاعدة البيانات موحدة ومتكاملة وجاهزة للاستخدام!** 🎊
