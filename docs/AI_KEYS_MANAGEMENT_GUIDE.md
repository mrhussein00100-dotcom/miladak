# 🔑 دليل إدارة مفاتيح API للذكاء الاصطناعي

## نظرة عامة

هذا الدليل يشرح كيفية إدارة وتغيير مفاتيح API لمزودي الذكاء الاصطناعي في موقع ميلادك.

---

## 📋 المفاتيح المطلوبة

| المزود      | اسم المتغير           | الحالة   | الاستخدام              |
| ----------- | --------------------- | -------- | ---------------------- |
| Gemini      | `GEMINI_API_KEY`      | ✅ مطلوب | توليد المقالات الرئيسي |
| Groq        | `GROQ_API_KEY`        | ✅ مطلوب | احتياطي سريع           |
| Pexels      | `PEXELS_API_KEY`      | ✅ مطلوب | صور المقالات           |
| Cohere      | `COHERE_API_KEY`      | اختياري  | احتياطي إضافي          |
| HuggingFace | `HUGGINGFACE_API_KEY` | اختياري  | احتياطي إضافي          |

---

## 🔧 طريقة تغيير المفاتيح

### الطريقة 1: عبر Vercel CLI (موصى بها)

```powershell
# 1. حذف المفتاح القديم
vercel env rm GEMINI_API_KEY production -y

# 2. إضافة المفتاح الجديد
echo "YOUR_NEW_API_KEY" | vercel env add GEMINI_API_KEY production

# 3. إعادة النشر لتفعيل التغييرات
vercel --prod
```

### الطريقة 2: عبر لوحة تحكم Vercel

1. افتح https://vercel.com/dashboard
2. اختر مشروع `miladak`
3. اذهب إلى **Settings** → **Environment Variables**
4. ابحث عن المتغير المطلوب (مثل `GEMINI_API_KEY`)
5. اضغط على **Edit** أو **Delete** ثم أضف الجديد
6. اضغط **Save**
7. أعد النشر: **Deployments** → **Redeploy**

---

## 🔑 الحصول على مفاتيح جديدة

### Gemini API Key

1. اذهب إلى: https://aistudio.google.com/apikey
2. سجل دخول بحساب Google
3. اضغط **Create API Key**
4. انسخ المفتاح (يبدأ بـ `AIza...`)

**النموذج المستخدم:** `gemini-2.5-flash` (الأحدث والأكثر استقراراً)

### Groq API Key

1. اذهب إلى: https://console.groq.com/keys
2. أنشئ حساب أو سجل دخول
3. اضغط **Create API Key**
4. انسخ المفتاح (يبدأ بـ `gsk_...`)

**النموذج المستخدم:** `llama-3.3-70b-versatile`

### Pexels API Key

1. اذهب إلى: https://www.pexels.com/api/
2. أنشئ حساب مجاني
3. اذهب إلى **Your API Key**
4. انسخ المفتاح

---

## 🧪 اختبار المفاتيح

### اختبار سريع عبر PowerShell

```powershell
# اختبار Gemini
$body = '{"contents":[{"parts":[{"text":"hi"}]}]}'
Invoke-RestMethod -Uri "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=YOUR_KEY" -Method POST -ContentType "application/json" -Body $body

# اختبار Groq
$headers = @{ "Authorization" = "Bearer YOUR_KEY"; "Content-Type" = "application/json" }
$body = '{"model":"llama-3.3-70b-versatile","messages":[{"role":"user","content":"hi"}],"max_tokens":10}'
Invoke-RestMethod -Uri "https://api.groq.com/openai/v1/chat/completions" -Headers $headers -Method POST -Body $body
```

### اختبار عبر API الموقع

```
GET https://miladak.com/api/admin/check-ai-keys
GET https://miladak.com/api/admin/test-ai
```

---

## 🔄 سلسلة الـ Fallback

عند فشل مزود، يتم الانتقال تلقائياً للمزود التالي:

```
Gemini → Groq → Cohere → HuggingFace → SONA (محلي)
```

**ملاحظة:** SONA هو المولد المحلي الذي يعمل بدون API خارجي.

---

## ⚠️ مشاكل شائعة وحلولها

### خطأ 429 (Rate Limit)

- **السبب:** تجاوز حد الاستخدام المجاني
- **الحل:** انتظر أو استخدم مفتاح جديد أو ترقية الخطة

### خطأ 401 (Unauthorized)

- **السبب:** مفتاح غير صحيح أو منتهي
- **الحل:** تحقق من المفتاح أو أنشئ مفتاح جديد

### خطأ 403 (Forbidden)

- **السبب:** المفتاح لا يملك صلاحيات كافية
- **الحل:** تحقق من إعدادات المفتاح في لوحة التحكم

---

## 📁 ملفات الإعدادات

| الملف             | الموقع | الاستخدام      |
| ----------------- | ------ | -------------- |
| `.env.local`      | محلي   | التطوير المحلي |
| `.env.production` | محلي   | مرجع للإنتاج   |
| Vercel Env        | سحابي  | الإنتاج الفعلي |

**مهم:** لا تضع المفاتيح الحقيقية في الملفات المحلية التي يتم رفعها لـ Git!

---

## 📊 مراقبة الاستخدام

### Gemini

- https://ai.dev/usage

### Groq

- https://console.groq.com/usage

---

## 🆘 الدعم

إذا واجهت مشاكل:

1. تحقق من صحة المفتاح
2. اختبر المفتاح مباشرة
3. راجع سجلات Vercel
4. تأكد من إعادة النشر بعد تغيير المفاتيح

---

_آخر تحديث: ديسمبر 2025_
